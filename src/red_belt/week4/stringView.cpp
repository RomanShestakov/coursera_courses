#include <string_view>
#include <vector>
#include <string>

namespace red_belt {

    std::vector<std::string_view> split( std::string_view s ) {
        std::vector< std::string_view > result;

        return result;

    }
}
